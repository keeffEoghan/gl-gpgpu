{
  "name": "gl-gpgpu",
  "version": "0.0.0",
  "description": "GPGPU current/past state stepping, mappings for shaders/resources/inputs/outputs over minimal passes/resources - BYORenderer.",
  "main": "index.js",
  "repository": "https://github.com/keeffEoghan/gl-gpgpu",
  "author": "Eoghan O'Keeffe <epok.tech@gmail.com> (https://epok.tech)",
  "license": "MIT",
  "engines": {
    "node": ">=12.14.1 || *",
    "yarn": ">=1.22.0 || *"
  },
  "scripts": {
    "start": "yarn run dev",
    "dev": "parcel --log-level verbose ./index.html",
    "build": "rm -rf dist/ && parcel build ./index.html -d ./dist/ --public-url ./dist/",
    "docs": "rm -rf docs/ && parcel build ./index.html -d ./docs/ --public-url ./"
  },
  "dependencies": {
    "@epok.tech/array-utils": "^0.0.4",
    "@thi.ng/vectors": "^4.7.0",
    "regl": "^2.0.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
