/**
 * The `gpgpu` inputs for `GL` `attribute`s.
 *
 * @module
 * @category JS
 */

/**
 * Default vertex positions `attribute`; 3 points of a large flat triangle.
 *
 * @see {@link attributes}
 */
export const positionsDef = [...positions];
